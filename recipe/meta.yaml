package:
  name: unicorn
  version: "2.1.4"

build:
  number: 1

source:
  url: https://files.pythonhosted.org/packages/source/u/unicorn/unicorn-2.1.4.tar.gz

requirements:
  build:
    - cmake 3.28.1
  host:
    - python

about:
  # The PyPI sdist doesn't carry a top-level license file, but the Unicorn
  # engine sources include the upstream GPLv2 text here.
  license_file: src/qemu/COPYING
